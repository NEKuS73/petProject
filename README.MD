# MyGoApp / Личный финансовый менеджер

Консольное приложение для учёта личных финансов, написанное на Go.

## 📁 Структура проекта (модульная архитектура)

<pre>
gogitproject/
├── cmd/
│   └── finapp/            # Точка входа приложения
│       ├── main.go
│       ├── data.json      # Файл с данными транзакций
│       └── finapp.exe     # Бинарник
├── internal/              # Внутренние пакеты
│   ├── models/            # Структуры данных (Transaction, Data)
│   │   └── models.go
│   ├── logic/             # Бизнес-логика (подсчёты, валидация)
│   │   └── logic.go
│   ├── storage/           # Работа с данными (чтение/запись JSON)
│   │   └── storage.go
│   └── ui/                # Пользовательский интерфейс (консольный ввод/вывод)
│       └── ui.go
├── go.mod                 # Модуль Go
└── README.md              # Этот файл
</pre>


## 🚀 Быстрый старт

1.  **Клонируйте репозиторий**
    ```bash
    (https://github.com/NEKuS73/petProject.git)
    ```
    ```bash
    (https://gitlab.com/NEKuS/petProject.git)
    ```
2.  **Убедитесь, что установлен Go** (версии 1.24 или выше):
    ```bash
    go version
    ```
3.  **Запустите приложение** из корневой папки проекта:
    ```bash
    go run cmd/finapp/main.go
    ```

## 🛠 Сборка и установка

Чтобы собрать исполняемый файл:
``` bash
# Сборка в текущую директорию
go build ./cmd/finapp

# Сборка с указанием имени и пути
go build -o bin/finapp ./cmd/finapp

# После этого запускайте:
./bin/finapp
```
## 📖 Использование
После запуска вы увидите главное меню:

=== Личный финансовый менеджер ===
1. Показать полный отчёт
2. Добавить транзакцию
3. Просмотреть список транзакций
4. Удалить транзакцию
5. Выйти из программы
Доступные функции:

Просмотр отчёта: Общий баланс и детализация по категориям доходов/расходов.

Добавление транзакции: Пошаговый ввод с проверкой даты, категории и суммы.

Просмотр списка: Таблица всех операций с фильтрацией.

Удаление транзакции: Удаление по ID с подтверждением.

## 🔧 Настройка
Файл данных: По умолчанию используется data.json в папке finapp/. При первом запуске, если файл не существует, он будет создан автоматически.

Категории: Список категорий доходов и расходов настраивается в internal/models/models.go.

## 🧪 Тестирование
Для запуска unit-тестов (предварительно нужно их создать в пакетах logic и storage):

```bash
# Запуск всех тестов в проекте
go test ./...

# Запуск тестов с просмотром покрытия
go test -cover ./...
```
## 📝 Дорожная карта (Roadmap)
Добавление unit-тестов для пакетов logic и storage

Поддержка бюджетирования (лимиты по категориям)

Экспорт отчётов в CSV/PDF

Веб-интерфейс (переход на архитектуру REST API)

Поддержка нескольких валют

Графики и визуализация статистики

## 👥 Вклад в проект
Если вы хотите улучшить проект:

Форкните репозиторий

Создайте ветку для новой функции (git checkout -b feature/amazing-feature)

Закоммитьте изменения (git commit -m 'feat: add some amazing feature')

Запушьте ветку (git push origin feature/amazing-feature)

Создайте Pull Request


Разработано как учебный проект для освоения Go и практики модульной архитектуры.

- Язык программирования Go установлен на вашем компьютере. Вы можете скачать его с [golang.org](https://golang.org/dl/)